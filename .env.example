# Environment Configuration
NODE_ENV=development

# Database Configuration (optionnel pour la version production)
# DATABASE_URL=postgresql://username:password@hostname:port/database

# Development Settings
PORT=5000
